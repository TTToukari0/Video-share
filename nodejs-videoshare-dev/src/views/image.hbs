<div class="card bg-dark text-white">
  <div class="card-header d-flex justify-content-between align-items-center">
    <h2 class="card-title">{{ image.title }}</h2>
    {{!-- <button class="btn btn-danger btn-sm" id="btn-delete" data-id="{{image.uniqueId}}">
      <i class="fa fa-times"></i> Delete
    </button> --}}
  </div>
  <div class="card-body">
    <p>{{ image.description }}</p>
    <div class="text-center">
    <video  class="img-fluid" style="height:auto;" src="/uploads/{{image.filename}}" id="video0" controls="controls"></video>

      {{!-- <video src="/uploads/{{image.filename}}" class="img-fluid" style="height:auto;"> --}}
    </div>
  </div>

</div>

<!-- COMMENTS -->

</div>